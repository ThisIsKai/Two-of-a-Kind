using UnityEngine;
using System.Collections;

//public class CameraFollowObject : MonoBehaviour {//This script just tells the game object it is attached to to follow
//												//another object every frame.
//
//	public Transform followObject; //transform var
//
//
//	void FixedUpdate () { //fixed update function
//		Vector3 currentPosition = transform.position; //our pos
//		currentPosition.x = Mathf.Lerp (currentPosition.x, followObject.position.x, -1); //lerp it for x
//		currentPosition.y = Mathf.Lerp (currentPosition.y, followObject.position.y, -1); //lerp it for y
//		transform.position = currentPosition; //assign the pos
//	}// END FIXED UPDATE
//}//END SCRIPT CAMERAFOLLOWOBJECT
